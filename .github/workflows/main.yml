name: Construir APK Android

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Baixar Código
      uses: actions/checkout@v4

    - name: Instalar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Instalar Dependências
      run: |
        pip install flet
        pip install pandas

    - name: Configurar Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Configurar Android SDK
      uses: android-actions/setup-android@v3

    # CORREÇÃO AQUI: Atualizado para a versão que o Flet exige (3.38.5)
    - name: Instalar Flutter (Motor do Flet)
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.38.5'

    - name: Construir APK
      # Adicionamos --no-android-checks para evitar perguntas interativas extras
      run: |
        flet build apk --verbose --no-android-checks

    - name: Upload do APK Gerado
      uses: actions/upload-artifact@v4
      with:
        name: AppLotofacil
        path: build/app/outputs/flutter-apk/app-release.apk
